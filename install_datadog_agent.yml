- name: Install Datadog Agent on servers in AWS
  hosts: all
  become: yes
  vars:
    datadog_api_key: "{{ api_key }}"
    datadog_config:
      tags:
        - env:dev
      logs_enabled: true
      process_config:
        enabled: "true"

  roles:
    - { role: Datadog.datadog, become: yes }
